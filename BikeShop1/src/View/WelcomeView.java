package View;

import java.util.ArrayList;
import java.util.Scanner;

import Model.BikeDatabaseModel;
import Model.BikeModel;
import Model.EbikeModel;
import Model.NormalBikeModel;

public class WelcomeView {
	

 /*public void showProducts() {
	
	Scanner input = new Scanner(System.in);
    BikeDatabaseModel db = new BikeDatabaseModel();
    db.addDefaultProducts();
          
    ArrayList<BikeModel> customerProducts = new ArrayList<BikeModel>();
    
    boolean quit = false;
    
    while (!quit) {
    System.out.print("1. Normal Bikes.\n2. Ebikes.\n3. Log Out\nPlease select what to do (1-3): ");
    switch (input.nextLine()) {
        case "1":
           
            for (NormalBikeModel normalbike : db.normalbikes)	{
            if (normalbike instanceof NormalBikeModel) 
                System.out.println(normalbike.toString());
            }
            break;
            
        case "2":
          
        	for (EbikeModel ebike : db.ebikes) {
                System.out.println(ebike.toString());
            }
            break;
        
        case "3":
            quit = true;
            break;
            
        default:
            break;
    }
    
    }  
} */

}